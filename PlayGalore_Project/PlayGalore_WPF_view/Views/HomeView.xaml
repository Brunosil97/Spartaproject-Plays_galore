<UserControl x:Class="PlayGalore_WPF_view.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlayGalore_WPF_view.Views"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <TextBlock FontSize="50">Home Page</TextBlock>

        <ListBox x:Name="PlayContainer"  Margin="10,65,10,10"
         ScrollViewer.VerticalScrollBarVisibility="Visible"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="PlayContainer_SelectionChanged">
            <!--<ListBox.ItemTemplate>
                <DataTemplate>-->
                    <!--<StackPanel Orientation="Horizontal">
                        <StackPanel Margin="10,0">
                            <TextBlock x:Name="PlayContent" FontWeight="Bold" />
                        </StackPanel>
                    </StackPanel>-->
                <!--</DataTemplate>
            </ListBox.ItemTemplate>-->
        </ListBox>
        <Button Content="Add A Play" HorizontalAlignment="Left" Margin="582,10,0,0" VerticalAlignment="Top" Height="50" Width="208" Click="Button_Click"/>
    </Grid>
</UserControl>
